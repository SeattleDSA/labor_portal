extends layout

block content
  .w3-container
    if locals.user
      h4 Browse the list of unions below.
      //TODO: get rid of iframe and use something secured by the airtable api.
      //We want to make sure that only slack authenticated users can edit or view
      //sensitive information.
      iframe(class="airtable-embed" src=env.UNIONS_AIRTABLE_EMBED frameborder="0" onmousewheel="" width="100%" height="533" style="background: transparent; border: 1px solid #ccc;")
    else
      h4 You are not logged in! Please #[a(href="/login") Log In] to continue.
